#include <iostream>
#include <cmath>
#include <vector>

using namespace std;
void test01(){
    vector<int> v;
    v.push_back(10);
    v.push_back(20);
    vector<int>::iterator itBegin = v.begin();
    vector<int>::iterator itEnd = v.end();
    for()
}
int main()
{
   
   return 0;
}
