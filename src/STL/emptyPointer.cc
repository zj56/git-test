#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

int main()
{
//    int* p = NULL;
//    *P = 100;
//    野指针 ：指针变量指向非法的内存空间
    // int* p = (int*)0x1100;
    // cout<<*P<<endl;
    // 野指针不是我们申请的空间，没有访问权限
    
   return 0;
}
