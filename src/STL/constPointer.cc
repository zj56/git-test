#include <iostream>
#include <cmath>
#include <vector>

using namespace std;
/* 
const修饰指针--常量指针 const int* p = &a
:指针的指向(p=&b)可以修改，但指针指向的值不可以修改(*p=20错误)
const修饰常量--指针常量 int* const p = &a
：指针的指向不可以改，指针指向的值可以改
const即修饰指针，又修饰常量  const int* const p =&a
:指针的指向不可以改，指针指向的值不可以改
*/
int main()
{
   int a = 10;
   int b = 
   return 0;
}
